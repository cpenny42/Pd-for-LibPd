You can use these soundfonts with the [soundfonts~] object:

	"set <full_path_to_soundfont.sf2>" will set the active soundfont, and a (note, velocity) pair will play a note.

	See soundfonts~-help.pd for more info.